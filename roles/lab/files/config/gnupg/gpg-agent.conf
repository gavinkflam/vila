# Use pinentry-curses to enter passphase
pinentry-program /usr/bin/pinentry-curses

# Cache passphase for 1 hour
max-cache-ttl 3600
max-cache-ttl-ssh 3600
default-cache-ttl 3600
default-cache-ttl-ssh 3600

# Act as a ssh agent as well
enable-ssh-support
